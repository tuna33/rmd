import React from 'react';

// import { RectangularPanelBody, RectangularPanelHeader } from '@components';

export const ManaCurve = (props) => {
	const deck = props.deck;

  return (
    <div
      style={{
        width: '50%',
        height: '87%',
      }}
    >
      Graph here for the deck
    </div>
  );
};